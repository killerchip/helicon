import { scenario } from '../../game/scenario/scenario.js';

export const initialState = {
    app: {
        gameStarted: false,
        config: {
            displayGrid: true
        }
    },
    scenario
};
